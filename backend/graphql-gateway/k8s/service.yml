apiVersion: v1
kind: Service
metadata:
  name: graphql-gateway
spec:
  type: NodePort
  selector:
    app: graphql-gateway
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
